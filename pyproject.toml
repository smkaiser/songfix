[project]
name = "songfix"
version = "0.1.0"
description = "Correct corrupted song and artist names"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn>=0.34,<1",
    "httpx>=0.28,<1",
    "openai>=1.60,<2",
]

[project.scripts]
songfix = "songfix.server:main"
